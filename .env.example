# Neon PostgreSQL connection string
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Brevo API Key (optional, if not set leads won't be sent but app works)
BREVO_API_KEY=""

# List of Brevo lists where leads go (optional, default: [2])
# BREVO_LIST_IDS="[2]"
